# Use the ultra-lightweight Nginx image
FROM nginx:alpine

# Copy the static HTML file to Nginx's default public serving directory
COPY index.html /usr/share/nginx/html/index.html

# Expose standard HTTP port 80
EXPOSE 80